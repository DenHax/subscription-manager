-- Drop subscriptions table first (due to foreign key constraints)
DROP TABLE IF EXISTS subscriptions.subscriptions;

-- Drop services table
DROP TABLE IF EXISTS subscriptions.services;

-- Drop users table
DROP TABLE IF EXISTS subscriptions.users;